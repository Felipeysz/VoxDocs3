<div class="modal fade" id="manualModal" tabindex="-1" aria-labelledby="manualModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-lg">
    <div class="modal-content rounded-4 shadow-lg animate__animated animate__fadeIn">
      <div class="modal-header bg-secondary text-white border-bottom-0 rounded-top-4 px-4 py-3">
        <h5 class="modal-title d-flex align-items-center gap-2 fs-4" id="manualModalLabel">
          <span class="material-symbols-outlined fs-3">menu_book</span>
          Manual do Usuário
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Fechar"></button>
      </div>
      <div class="modal-body px-4 py-4">
        <p class="fs-5 mb-4 text-center fw-semibold">
          <span class="material-symbols-outlined align-middle text-secondary me-1">info</span>
          Siga os passos abaixo para montar corretamente o código de acesso ao documento:
        </p>
        <div class="row g-3">
          <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm rounded-3">
              <div class="card-body text-center">
                <span class="material-symbols-outlined fs-1 text-secondary mb-2">description</span>
                <h6 class="fw-bold mb-2">1. Código do Documento</h6>
                <p class="small text-muted mb-1">Ex: <code>DOC</code></p>
                <p class="small">Informe o identificador do documento.</p>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm rounded-3">
              <div class="card-body text-center">
                <span class="material-symbols-outlined fs-1 text-dark mb-2">category</span>
                <h6 class="fw-bold mb-2">2. Tipo do Documento</h6>
                <p class="small text-muted mb-1">Ex: <code>CONT</code></p>
                <p class="small">Escolha o tipo definido na etapa anterior.</p>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm rounded-3">
              <div class="card-body text-center">
                <span class="material-symbols-outlined fs-1 text-muted mb-2">calendar_month</span>
                <h6 class="fw-bold mb-2">3. Mês e Ano</h6>
                <p class="small text-muted mb-1">Ex: <code>042025</code></p>
                <p class="small">Informe no formato MÊS+ANO (MMYYYY).</p>
              </div>
            </div>
          </div>
        </div>
        <div class="mt-5 text-center">
          <p class="fs-6 text-muted mb-2">Exemplo completo de código gerado:</p>
          <div class="bg-light border border-secondary-subtle rounded-pill d-inline-block px-4 py-2 fs-5 fw-semibold text-dark">
            DOCCONT042025
          </div>
        </div>
      </div>
    </div>
  </div>
</div>